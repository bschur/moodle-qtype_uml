{
  "name": "qtype_uml",
  "type": "module",
  "scripts": {
    "build": "rm -rf ./amd && mkdir -p ./amd/src && rsync -r --include '*/' --include '*.js' --include '.eslintrc' --exclude '*' ./editor/ ./amd/src/ && npm run replace:jsending && npm run replace:addmoodlenamespace && npm run build:moodle",
    "build:moodle": "npx grunt amd",
    "replace:jsending": "replace \".js'\" \"'\" ./amd/src -r",
    "replace:addmoodlenamespace": "replace \"from './\" \"from 'qtype_uml/\" ./amd/src -r"
  },
  "devDependencies": {
    "replace": "^1.2.2"
  }
}
