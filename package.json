{
  "name": "qtype_uml",
  "type": "module",
  "scripts": {
    "build": "mkdir -p ./amd && rm -rf ./amd/**/* && npm run convert:es6toes5 && npm run build:moodle #&& rm -rf ./amd/src",
    "convert:es6toes5": "npx babel ./editor --out-dir ./amd/src",
    "build:moodle": "npx grunt rollup"
  },
  "devDependencies": {
    "@babel/cli": "^7.23.4",
    "@babel/core": "^7.23.5",
    "babel-plugin-search-and-replace": "^1.1.1"
  }
}
